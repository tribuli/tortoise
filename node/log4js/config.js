/**
 * configuration - `log4js.configure(object || string)`
 * A string argument is treated as a filename to load configuration from.
 * Config files should be JSON, Config files should be JSON, and contain a configuration object.
 * You can also pass a configuration object directly to `configure`.
 */
const config = {
  // 非必填项
  "levels": {
    "info": {}
  }
}